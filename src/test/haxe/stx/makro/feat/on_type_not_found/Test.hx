package stx.makro.feat.on_type_not_found;

using stx.Nano;
using stx.Test;
using stx.Log;
using stx.Assert;

class Test{
  static public function main(){
    //final q = new stx.assert.eq.gen.eu.home

    __.test().run(
      [

      ],[]
    );
  }
}