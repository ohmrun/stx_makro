package stx.makro.feat.module_type_cleverness;

using stx.Nano;
using stx.Test;
using stx.Log;

import stx.makro.feat.module_type_cleverness.Moduler.Noth;

class Test extends TestCase{
  static public function main(){
    __.test().run(
      [
        new TestCase()
      ],[]
    );
  }
  public function test(){
    
  }
}