package stx.macro.expr.body;

class FieldTypes{
	/*
    public static function eq(l:FieldType,r:FieldType){
        return switch([l,r]){
            case [FVar(lt,le),FVar(rt,re)]  : ComplexTypes.eq(lt,rt) && Exprs.eq(le,re);
            case [FFun(lf),FFun(rf)]        : Functions.eq(lf,rf);
        }
        FVar( t :ComplexType>, ?e : Null<Expr> );
	//	Represents a function field type.
	
	FFun( f : Function );

	//	Represents a property with getter and setter field type.
	
	FProp( get : String, set : String, ?t : Null<ComplexType>, ?e : Null<Expr> );
    }
	*/
}
