package stx.makro;

import haxe.macro.Context;


class ClassList {
  static var NAME = 'stx.makro.ClassList';//must be the fully qualified name of this class
  
   static function build() {
    var fields = [];
    return fields;
  }
}