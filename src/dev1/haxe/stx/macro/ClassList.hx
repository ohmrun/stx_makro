package stx.macro;

import haxe.macro.Context;
using stx.simplex.Package;
using stx.arrowlet.Package;

class ClassList {
  static var NAME = 'stx.macro.ClassList';//must be the fully qualified name of this class
  
   static function build() {
    var fields = [];
    return fields;
  }
}